import React from 'react';
import { Logo } from '../Logo';
import { StyledHeader } from './styles';

export const Header = () => {
  return (
    <StyledHeader>
      <Logo />
    </StyledHeader>
  );
};
