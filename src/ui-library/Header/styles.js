import styled from 'styled-components';
import { brandColors } from '../brand/colors';

export const StyledHeader = styled.header`
  padding: 0.5em 0;
  text-align: center;
  background-color: ${brandColors.grey2};
`;
